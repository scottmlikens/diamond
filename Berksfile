site :opscode

metadata

cookbook "python"
cookbook "git"
cookbook "build-essential"
cookbook "runit","1.1.0"

group :integration do
  cookbook "apt"
  cookbook "yum"
  cookbook "minitest-handler"
end